#include <stdio.h>

/*
 * HACK: Your Program's entry point is a function called "main".
 * The return type can be anything, however most operating systems expect a
 * return type can be anything, however most operating systems expect a return
 * type of 'int' for error coding processing
 */
int main(void) {
  printf("Hello from the main function");

  /*
   * NOTE: Here we are returning the exit code 0 to tell the Operating System
   * that the program execution is successful.
   */
  return 0;
}
